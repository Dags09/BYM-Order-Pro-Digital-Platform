bugs found

1. when refreshing the web page it logs out the user, it should not log out the user use a timer like if 1 hr have pass it automatically logs out the user

2. when clicking the "start ordering" button it automatically logs in the user (maybe it use the reference of the last log in, which mean the user maybe log in using other users profile) - new discovery (this bug will happen if the #1 bug happens)

3. when logging out while it has a selected product to pruchased which will be seen in the cart, then logging in that item are still remain in their( it should be gone if logging out and list will be gone)

improvement needed:

the commented is pending

<!-- 1. in shop page it needs a view (tiles, tables, etc) both in computer and mobile
2. in cart beside the trash icon their should be an edit icon where it allows to edit the value, which mean the input field will not appear until it was click, also when the edit icon is click it will be change to "save" icon or text and clicking it will you know
3. in shop the categories should be view no more than 5 then the six button should be more, where clicking it will (option 1. change a page to category page, and when selecting on it view only the product within the category) (option 2. a card containing the full list of category will appear in front like in the middile if the screen and center of it and the background will be blured)

4. the bg needs improvement in mobile, which mean it needs to be responsive( it needs to move to left when in mobile, so that the woman chartacter can be seen)

5. in homepage, in product section it need to be a smaller card of it like the partner ship card section

6. needs an animations scroll -->

1. in order details (manager) the payment evidence should shown
